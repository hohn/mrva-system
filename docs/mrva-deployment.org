#+LATEX_COMPILER: lualatex

#+LATEX_HEADER: \usepackage{minted}
#+LATEX_HEADER: \usepackage{amssymb}
#+LATEX_HEADER: \usepackage{xcolor}
#+LATEX_HEADER: \usepackage{newunicodechar}

#+LATEX_HEADER: \usepackage{newunicodechar}

#+LATEX_HEADER: \newunicodechar{₁}{\ensuremath{_1}}
#+LATEX_HEADER: \newunicodechar{₂}{\ensuremath{_2}}
#+LATEX_HEADER: \newunicodechar{₃}{\ensuremath{_3}}
#+LATEX_HEADER: \newunicodechar{₄}{\ensuremath{_4}}
#+LATEX_HEADER: \newunicodechar{₅}{\ensuremath{_5}}
#+LATEX_HEADER: \newunicodechar{₆}{\ensuremath{_6}}
#+LATEX_HEADER: \newunicodechar{₇}{\ensuremath{_7}}
#+LATEX_HEADER: \newunicodechar{₈}{\ensuremath{_8}}
#+LATEX_HEADER: \newunicodechar{₉}{\ensuremath{_9}}
#+LATEX_HEADER: \newunicodechar{₀}{\ensuremath{_0}}

#+LATEX_HEADER: \newunicodechar{ₐ}{\ensuremath{_a}}
#+LATEX_HEADER: \newunicodechar{ₑ}{\ensuremath{_e}}
#+LATEX_HEADER: \newunicodechar{ₒ}{\ensuremath{_o}}
#+LATEX_HEADER: \newunicodechar{ₓ}{\ensuremath{_x}}
#+LATEX_HEADER: \newunicodechar{ₙ}{\ensuremath{_n}}
#+LATEX_HEADER: \newunicodechar{ₖ}{\ensuremath{_k}}
#+LATEX_HEADER: \newunicodechar{ₘ}{\ensuremath{_m}}
#+LATEX_HEADER: \newunicodechar{≡}{\ensuremath{\equiv}}
#+LATEX_HEADER: \newunicodechar{…}{\ensuremath{\ldots}}

#+LATEX_HEADER: \newunicodechar{≈}{\ensuremath{\mathrel{\approx}}}
#+LATEX_HEADER: \newunicodechar{↓}{\ensuremath{\mathrel{\downarrow}}}
#+LATEX_HEADER: \newunicodechar{✅}{\textcolor{green!50!black}{\ensuremath{\checkmark}}}
#+LATEX_HEADER: \newunicodechar{❌}{\textcolor{red!70!black}{\ensuremath{\times}}}
#+LATEX_HEADER: \newunicodechar{∣}{\ensuremath{\mid}}
#+LATEX_HEADER: \newunicodechar{∈}{\ensuremath{\in}}
#+LATEX_HEADER: \newunicodechar{→}{\ensuremath{\to}}
#+LATEX_HEADER: \newunicodechar{⇒}{\ensuremath{\Rightarrow}}
#+LATEX_HEADER: \newunicodechar{↦}{\ensuremath{\mapsto}}
#+LATEX_HEADER: \newunicodechar{≥}{\ensuremath{\ge}}
#+LATEX_HEADER: \newunicodechar{≤}{\ensuremath{\le}}
#+LATEX_HEADER: \newunicodechar{≡}{\ensuremath{\equiv}}
#+LATEX_HEADER: \newunicodechar{⊂}{\ensuremath{\subset}}
#+LATEX_HEADER: \newunicodechar{⊃}{\ensuremath{\supset}}
#+LATEX_HEADER: \newunicodechar{⊆}{\ensuremath{\subseteq}}
#+LATEX_HEADER: \newunicodechar{⊇}{\ensuremath{\supseteq}}
#+LATEX_HEADER: \newunicodechar{⟶}{\ensuremath{\longrightarrow}}
#+LATEX_HEADER: \newunicodechar{⟵}{\ensuremath{\longleftarrow}}
#+LATEX_HEADER: \newunicodechar{⋯}{\ensuremath{\cdots}}
#+LATEX_HEADER: \newunicodechar{…}{\ensuremath{\ldots}}
#+LATEX_HEADER: \newunicodechar{∪}{\ensuremath{\cup}}
#+LATEX_HEADER: \newunicodechar{∩}{\ensuremath{\cap}}
#+LATEX_HEADER: \newunicodechar{∧}{\ensuremath{\wedge}}
#+LATEX_HEADER: \newunicodechar{∨}{\ensuremath{\vee}}
#+LATEX_HEADER: \newunicodechar{¬}{\ensuremath{\neg}}
#+LATEX_HEADER: \newunicodechar{∀}{\ensuremath{\forall}}
#+LATEX_HEADER: \newunicodechar{∃}{\ensuremath{\exists}}
#+LATEX_HEADER: \newunicodechar{⊢}{\ensuremath{\vdash}}
#+LATEX_HEADER: \newunicodechar{⊨}{\ensuremath{\vDash}}
#+LATEX_HEADER: \newunicodechar{⊥}{\ensuremath{\bot}}
#+LATEX_HEADER: \newunicodechar{⊤}{\ensuremath{\top}}
#+LATEX_HEADER: \newunicodechar{∅}{\ensuremath{\emptyset}}
#+LATEX_HEADER: \newunicodechar{↔}{\ensuremath{\leftrightarrow}}
#+LATEX_HEADER: \newunicodechar{⇔}{\ensuremath{\Leftrightarrow}}

#+LATEX_HEADER: \usepackage[
#+LATEX_HEADER:   paper=letterpaper,
#+LATEX_HEADER:   left=1in,
#+LATEX_HEADER:   right=1in,
#+LATEX_HEADER:   top=1in,
#+LATEX_HEADER:   bottom=1in
#+LATEX_HEADER: ]{geometry}

#+LATEX_HEADER: \usepackage{fontspec}
#+LATEX_HEADER: \setmainfont{Avenir Next}[
#+LATEX_HEADER:   UprightFont = *,
#+LATEX_HEADER:   ItalicFont = * Italic,
#+LATEX_HEADER:   BoldFont = * Demi Bold,
#+LATEX_HEADER:   BoldItalicFont = * Demi Bold Italic
#+LATEX_HEADER: ]

# #+LATEX_HEADER: \usepackage{fontspec}
# #+LATEX_HEADER: \setmainfont{CMU Bright}
# #+LATEX_HEADER: \setsansfont{CMU Bright}
# #+LATEX_HEADER: \setmonofont{CMU Typewriter Text}

#+HTML_HEAD: <style>
#+HTML_HEAD: body {
#+HTML_HEAD:   font-family: "Avenir Next", "Avenir", "Average Sans", "Helvetica Neue", Helvetica, Arial, sans-serif;
#+HTML_HEAD:   font-weight: 400;
#+HTML_HEAD:   line-height: 1.5;
#+HTML_HEAD: }
#+HTML_HEAD: code, pre, tt {
#+HTML_HEAD:   font-family: "CMU Typewriter Text", "CMU Typewriter", "Courier New", monospace;
#+HTML_HEAD:   font-size: 0.95em;
#+HTML_HEAD:   background-color: #f8f8f8;
#+HTML_HEAD:   border-radius: 4px;
#+HTML_HEAD:   padding: 0.1em 0.25em;
#+HTML_HEAD: }
#+HTML_HEAD: pre {
#+HTML_HEAD:   padding: 0.6em;
#+HTML_HEAD:   border: 1px solid #ddd;
#+HTML_HEAD:   background-color: #fdfdfd;
#+HTML_HEAD:   overflow-x: auto;
#+HTML_HEAD: }
#+HTML_HEAD: </style>

# #+OPTIONS: author:nil date:nil html-validation-link:nil
# #+OPTIONS: creator:nil
# #+OPTIONS: html-postamble:nil
#+OPTIONS: H:2 num:t \n:nil @:t ::t |:t ^:{} f:t *:t TeX:t LaTeX:t skip:nil p:nil

* MRVA deployment documentation

** Introduction
  This document describes the /deployment-level/ structure of MRVA.  The goal is to
  explain /what/ is deployed and /how the pieces relate/, without descending into
  developer-level or implementation details.

  We therefore stay strictly at the /container level/.  The internal structure of
  each container is fully specified by its =Dockerfile=; those details are treated
  as authoritative and are not duplicated here.  The complete, reproducible
  assembly process—starting from source—is described in the
  [[*MRVA Build Order][MRVA Build Order]] section.

  The canonical list of containers used in a reference deployment is defined in

  - [[../submodules/mrva-docker/docker-compose-demo.yml]]

  with corresponding Dockerfiles located in

  - [[../submodules/mrva-docker/containers/]]

  Only containers that are part of the MRVA system itself are discussed in this
  document; stock infrastructure containers are treated as external services.

** Container groups

   For clarity, containers are grouped by responsibility:

   1. *Internal containers*: MRVA proper
   2. *Stock containers*: off-the-shelf services used by MRVA
   3. *Externally provided services*: required services that are expected to be
      supplied by the deployment environment (a reference implementation is
      provided for convenience)

   This grouping reflects responsibility boundaries rather than implementation
   details.

** Internal containers: MRVA proper

   The following repositories are submodules of this repository
   ([[../submodules/]]) and together constitute MRVA itself:

   - =gh-mrva=
   - =mrvaserver=
   - =mrvaagent=
   - =mrvacommander=
   - =mrvahepc= (reference implementation only; see below)

   Their roles are as follows:

   - =gh-mrva=
     A user-driven, shell-oriented frontend providing three operations:
     1. submit analysis requests
     2. query request status
     3. download results (and optionally CodeQL databases)

   - =mrvaserver=
     The externally facing service.  It receives requests from =gh-mrva= and
     orchestrates their execution by coordinating the internal services.

   - =mrvaagent=
     The internal job runner.  It performs the actual analyses:
     - consumes jobs
     - retrieves database metadata (and possibly databases) via =mrvahepc=
     - runs =codeql database analyze=
     - returns results via RabbitMQ

   - =mrvacommander=
     Shared library code used by both =mrvaserver= and =mrvaagent=.  It contains no
     standalone service logic.

   - =mrva-docker=
     The container-assembly repository that defines how all MRVA containers are
     built and composed:
     https://github.com/hohn/mrva-docker

** Stock containers (infrastructure services)

   These are unmodified, publicly available containers that provide standard
   infrastructure services within the MRVA deployment:

   - =postgres=
   - =rabbitmq=
   - =minio=

   They are treated as replaceable components, provided they satisfy the expected
   interfaces.

** Externally provided services

   Some services are required by MRVA but are expected to be supplied externally in
   real deployments.  A reference implementation is provided only to make the
   system self-contained.

   - =mrvahepc=
     Endpoint for CodeQL database metadata and (optionally) database content.

     This container is provided *only as an example*.  In production deployments,
     this service is expected to be external to MRVA.

     Responsibilities:
     - serve CodeQL database metadata inside the container network
     - optionally serve the actual CodeQL databases
       (the reference implementation uses an external HTTPS server for DB content)
     - act as a replaceable, API-stable component

     Reference implementation:
     - https://github.com/hohn/mrvahepc/tree/master

*** Fixed API contract of =mrvahepc=

    The =mrvahepc= service exposes a *fixed API* that MRVA relies on.

    - Metadata endpoint:
      - returns a JSON array
      - schema definition:
        https://github.com/hohn/mrvahepc/blob/master/bin/mc-hepc-init#L65
      - example payload:

        #+begin_src javascript
          [
              {
                  "git_branch": "HEAD",
                  "git_commit_id": "dc0c7f3de40530053189c572936ae4fd1567269b",
                  "git_repo": "bulk-builder",
                  "ingestion_datetime_utc": "2022-11-30T00:44:34.455172576Z",
                  "result_url": "http://hepc/db/db-collection.tmp/bulk-builder-bulk-builder-ctsj-a8e72a.zip",
                  "tool_id": "9f2f9642-febb-4435-9204-fb50bbd43de4",
                  "tool_name": "codeql-cpp",
                  "tool_version": "2.11.3",
                  "projname": "bulk-builder/bulk-builder"
              }
          ]
        #+end_src

    - Content serving:
      - actual database content is referenced via the metadata
      - the metadata field name is fixed:
        - ="result_url"=
      - the URL itself is arbitrary and deployment-specific, e.g.:

        ="http://hepc/db/db-collection.tmp/bulk-builder-bulk-builder-ctsj-a8e72a.zip"=

    As long as this API contract is respected, =mrvahepc= can be freely replaced.

** Container assembly

   The assembly and composition of all containers is driven by the
   =mrva-docker= repository.  This deployment document intentionally stops at the
   container boundary; build mechanics and developer-level concerns are covered
   elsewhere.

* MRVA Build Order
  This document defines execution order.
  Scripts define state; this document defines sequence.

  1. prepare-scratch.sh
  2. build-gh-mrva.sh
  3. build-mrvaserver.sh
  4. build-mrvaagent.sh
  5. build-mrvahepc.sh

  All scripts:
  - are self-locating
  - write only into deploy-scratch/state
  - emit logs and verification markers
  - may be re-run once prerequisites exist
** TODO add mrva hepc loading steps
** TODO docker inspect --format '{{.Os}}/{{.Architecture}}' mrva-gh-mrva:0.4.7
** TODO add mrva-platform image with all tools
   # https://chatgpt.com/g/g-p-67b4e0777f708191a749244e016fe7e8/c/69652e16-0038-832b-97df-f54470f307cd
   # abstraction: a “platform base image

   - MRVA services differ only by what they do, never by how the platform beneath
     them is configured.

   - No Docker image reference may exist without an explicit version or digest.
     
   - [X] combine common Docker image tools into one base image, mrva-platform
     gpt: https://chatgpt.com/g/g-p-67b4e0777f708191a749244e016fe7e8-mrva/c/69652e16-0038-832b-97df-f54470f307cd
     Goal:
     - combine common Docker image tools into one base image, mrva-platform
       - get the CODEQL_VERSION from the spec file
       - use ubuntu:24.04 as base 
       - include Golang, version 1.25.5, in mrva-platform
       - use python 3.11

     - replace the individual Dockerfiles with simple additions to the base image
       
     - output:
       - replacement Dockerfiles
       - new Dockefile
       - as shar archive, plain text
       - to extract into
         #+BEGIN_SRC text
           hohn@thelio24 ~/work-gh/mrva/mrva-system/submodules/mrva-docker/containers]$
           0:$ ls -lF
           total 48
           drwxrwxr-x 1 hohn hohn    82 Jan 15 10:24 agent/
           drwxrwxr-x 1 hohn hohn    20 Jan 15 10:24 aptcacher/
           drwxrwxr-x 1 hohn hohn    76 Jan 15 10:24 dbsdata/
           -rw-r--r-- 1 hohn hohn 12547 Jan 15 12:26 foo.shar
           drwxrwxr-x 1 hohn hohn    56 Jan 15 10:24 ghmrva/
           drwxrwxr-x 1 hohn hohn    40 Jan 15 10:24 hepc/
           drwxrwxr-x 1 hohn hohn    40 Jan 15 10:24 node-build/
           drwxr-xr-x 1 hohn hohn     0 Jan 15 11:22 platform/
           drwxrwxr-x 1 hohn hohn    56 Jan 15 10:24 qldbtools/
           drwxrwxr-x 1 hohn hohn   108 Jan 15 10:24 server/
           drwxrwxr-x 1 hohn hohn    88 Jan 15 10:24 vscode/
         #+END_SRC

     The directories and Dockerfiles follow.

     Ask if anything is unclear.
     #+BEGIN_SRC sh 
       [hohn@thelio24 ~/work-gh/mrva/mrva-system/submodules/mrva-docker/containers]$
       0:$ ls agent/
       Dockerfile  entrypoint.sh  Makefile  README.org
       [hohn@thelio24 ~/work-gh/mrva/mrva-system/submodules/mrva-docker/containers]$
       0:$ ls agent/ server/ ghmrva/ hepc/
       agent/:
       Dockerfile  entrypoint.sh  Makefile  README.org

       ghmrva/:
       Dockerfile  Makefile  README.org

       hepc/:
       Dockerfile  README.org

       server/:
       Dockerfile  entrypoint.sh  Makefile  mcconfig.toml  README.org
       [hohn@thelio24 ~/work-gh/mrva/mrva-system/submodules/mrva-docker/containers]$



       #!/bin/sh
       # This is a shell archive (produced by GNU sharutils 4.15.2).
       # To extract the files from this archive, save it to some FILE, remove
       # everything before the '#!/bin/sh' line above, then type 'sh FILE'.
       #
       lock_dir=_sh07323
       # Made on 2026-01-15 12:26 PST by <hohn@thelio24>.
       # Source directory was '/mnt/common/home/hohn/work-gh/mrva/mrva-system/submodules/mrva-docker/containers'.
       #
       # Existing files will *not* be overwritten, unless '-c' is specified.
       #
       # This shar contains:
       # length mode       name
       # ------ ---------- ------------------------------------------
       #   1566 -rw-rw-r-- agent/Dockerfile
       #   1593 -rw-rw-r-- server/Dockerfile
       #   1591 -rw-rw-r-- ghmrva/Dockerfile
       #    631 -rw-rw-r-- hepc/Dockerfile
       #
       MD5SUM=${MD5SUM-md5sum}
       f=`${MD5SUM} --version | egrep '^md5sum .*(core|text)utils'`
       test -n "${f}" && md5check=true || md5check=false
       ${md5check} || \
         echo 'Note: not verifying md5sums.  Consider installing GNU coreutils.'
       if test "X$1" = "X-c"
       then keep_file=''
       else keep_file=true
       fi
       echo=echo
       save_IFS="${IFS}"
       IFS="${IFS}:"
       gettext_dir=
       locale_dir=
       set_echo=false

       for dir in $PATH
       do
         if test -f $dir/gettext \
            && ($dir/gettext --version >/dev/null 2>&1)
         then
           case `$dir/gettext --version 2>&1 | sed 1q` in
             ,*GNU*) gettext_dir=$dir
             set_echo=true
             break ;;
           esac
         fi
       done

       if ${set_echo}
       then
         set_echo=false
         for dir in $PATH
         do
           if test -f $dir/shar \
              && ($dir/shar --print-text-domain-dir >/dev/null 2>&1)
           then
             locale_dir=`$dir/shar --print-text-domain-dir`
             set_echo=true
             break
           fi
         done

         if ${set_echo}
         then
           TEXTDOMAINDIR=$locale_dir
           export TEXTDOMAINDIR
           TEXTDOMAIN=sharutils
           export TEXTDOMAIN
           echo="$gettext_dir/gettext -s"
         fi
       fi
       IFS="$save_IFS"
       if (echo "testing\c"; echo 1,2,3) | grep c >/dev/null
       then if (echo -n test; echo 1,2,3) | grep n >/dev/null
            then shar_n= shar_c='
       '
            else shar_n=-n shar_c= ; fi
       else shar_n= shar_c='\c' ; fi
       f=shar-touch.$$
       st1=200112312359.59
       st2=123123592001.59
       st2tr=123123592001.5 # old SysV 14-char limit
       st3=1231235901

       if   touch -am -t ${st1} ${f} >/dev/null 2>&1 && \
            test ! -f ${st1} && test -f ${f}; then
         shar_touch='touch -am -t $1$2$3$4$5$6.$7 "$8"'

       elif touch -am ${st2} ${f} >/dev/null 2>&1 && \
            test ! -f ${st2} && test ! -f ${st2tr} && test -f ${f}; then
         shar_touch='touch -am $3$4$5$6$1$2.$7 "$8"'

       elif touch -am ${st3} ${f} >/dev/null 2>&1 && \
            test ! -f ${st3} && test -f ${f}; then
         shar_touch='touch -am $3$4$5$6$2 "$8"'

       else
         shar_touch=:
         echo
         ${echo} 'WARNING: not restoring timestamps.  Consider getting and
       installing GNU '\''touch'\'', distributed in GNU coreutils...'
         echo
       fi
       rm -f ${st1} ${st2} ${st2tr} ${st3} ${f}
       #
       if test ! -d ${lock_dir} ; then :
       else ${echo} "lock directory ${lock_dir} exists"
            exit 1
       fi
       if mkdir ${lock_dir}
       then ${echo} "x - created lock directory ${lock_dir}."
       else ${echo} "x - failed to create lock directory ${lock_dir}."
            exit 1
       fi
       # ============= agent/Dockerfile ==============
       if test ! -d 'agent'; then
         mkdir 'agent'
       if test $? -eq 0
       then ${echo} "x - created directory agent."
       else ${echo} "x - failed to create directory agent."
            exit 1
       fi
       fi
       if test -n "${keep_file}" && test -f 'agent/Dockerfile'
       then
       ${echo} "x - SKIPPING agent/Dockerfile (file already exists)"

       else
       ${echo} "x - extracting agent/Dockerfile (text)"
         sed 's/^X//' << 'SHAR_EOF' > 'agent/Dockerfile' &&
       # Runtime container - binary built on host
       FROM ubuntu:24.04
       ENV DEBIAN_FRONTEND=noninteractive
       X
       # Build argument for CodeQL version, defaulting to the latest release
       ARG CODEQL_VERSION=latest
       X
       # Install packages
       RUN apt-get update && apt-get install --no-install-recommends --assume-yes \
       X    unzip curl ca-certificates default-jdk && \
       X    apt-get clean && rm -rf /var/lib/apt/lists/*
       X
       # If the version is 'latest', lsget the latest release version from GitHub, unzip the bundle into /opt, and delete the archive
       RUN if [ "$CODEQL_VERSION" = "latest" ]; then \
       X    CODEQL_VERSION=$(curl -s https://api.github.com/repos/github/codeql-cli-binaries/releases/latest | grep '"tag_name"' | sed -E 's/.*"([^"]+)".*/\1/'); \
       X    fi && \
       X    echo "Using CodeQL version $CODEQL_VERSION" && \
       X    curl -L "https://github.com/github/codeql-cli-binaries/releases/download/$CODEQL_VERSION/codeql-linux64.zip" -o /tmp/codeql.zip && \
       X    unzip /tmp/codeql.zip -d /opt && \
       X    rm /tmp/codeql.zip && \
       X    chmod -R +x /opt/codeql
       X
       # Set environment variables for CodeQL
       ENV CODEQL_CLI_PATH=/opt/codeql/codeql
       X
       # Set environment variable for CodeQL for `codeql database analyze` support on ARM
       # This env var has no functional effect on CodeQL when running on x86_64 linux
       ENV CODEQL_JAVA_HOME=/usr
       X
       # Copy the host-built binary
       COPY mrvaagent /usr/local/bin/mrvaagent
       X
       # Copy the binary-replacement support script to the container
       COPY entrypoint.sh /usr/local/bin/entrypoint.sh
       X
       # Run the agent with the default mode set to container
       ENTRYPOINT ["/usr/local/bin/entrypoint.sh"]
       X
       SHAR_EOF
         (set 20 26 01 12 14 04 08 'agent/Dockerfile'
          eval "${shar_touch}") && \
         chmod 0664 'agent/Dockerfile'
       if test $? -ne 0
       then ${echo} "restore of agent/Dockerfile failed"
       fi
         if ${md5check}
         then (
              ${MD5SUM} -c >/dev/null 2>&1 || ${echo} 'agent/Dockerfile': 'MD5 check failed'
              ) << \SHAR_EOF
       0ee3405d176b9203eedae8fc7695e375  agent/Dockerfile
       SHAR_EOF

       else
       test `LC_ALL=C wc -c < 'agent/Dockerfile'` -ne 1566 && \
         ${echo} "restoration warning:  size of 'agent/Dockerfile' is not 1566"
         fi
       fi
       # ============= server/Dockerfile ==============
       if test ! -d 'server'; then
         mkdir 'server'
       if test $? -eq 0
       then ${echo} "x - created directory server."
       else ${echo} "x - failed to create directory server."
            exit 1
       fi
       fi
       if test -n "${keep_file}" && test -f 'server/Dockerfile'
       then
       ${echo} "x - SKIPPING server/Dockerfile (file already exists)"

       else
       ${echo} "x - extracting server/Dockerfile (text)"
         sed 's/^X//' << 'SHAR_EOF' > 'server/Dockerfile' &&
       # Runtime container - binary built on host
       FROM ubuntu:24.04
       ENV DEBIAN_FRONTEND=noninteractive
       X
       # Build argument for CodeQL version, defaulting to the latest release
       ARG CODEQL_VERSION=latest
       X
       # Install packages
       RUN apt-get update && apt-get install --no-install-recommends --assume-yes \
       X    unzip curl ca-certificates default-jdk && \
       X    apt-get clean && rm -rf /var/lib/apt/lists/*
       X
       # If the version is 'latest', lsget the latest release version from GitHub, unzip the bundle into /opt, and delete the archive
       RUN if [ "$CODEQL_VERSION" = "latest" ]; then \
       X    CODEQL_VERSION=$(curl -s https://api.github.com/repos/github/codeql-cli-binaries/releases/latest | grep '"tag_name"' | sed -E 's/.*"([^"]+)".*/\1/'); \
       X    fi && \
       X    echo "Using CodeQL version $CODEQL_VERSION" && \
       X    curl -L "https://github.com/github/codeql-cli-binaries/releases/download/$CODEQL_VERSION/codeql-linux64.zip" -o /tmp/codeql.zip && \
       X    unzip /tmp/codeql.zip -d /opt && \
       X    rm /tmp/codeql.zip && \
       X    chmod -R +x /opt/codeql
       X
       # Set environment variables for CodeQL
       ENV CODEQL_CLI_PATH=/opt/codeql/codeql
       X
       # Set environment variable for CodeQL for `codeql database analyze` support on ARM
       # This env var has no functional effect on CodeQL when running on x86_64 linux
       ENV CODEQL_JAVA_HOME=/usr
       X
       # Copy the host-built binary
       COPY mrvaserver /usr/local/bin/mrvaserver
       X
       # Copy the binary-replacement support script to the container
       COPY entrypoint.sh /usr/local/bin/entrypoint.sh
       X
       # Run the server with the default mode set to container
       ENTRYPOINT ["/usr/local/bin/entrypoint.sh"]
       CMD ["--mode=container"]
       SHAR_EOF
         (set 20 26 01 12 14 04 09 'server/Dockerfile'
          eval "${shar_touch}") && \
         chmod 0664 'server/Dockerfile'
       if test $? -ne 0
       then ${echo} "restore of server/Dockerfile failed"
       fi
         if ${md5check}
         then (
              ${MD5SUM} -c >/dev/null 2>&1 || ${echo} 'server/Dockerfile': 'MD5 check failed'
              ) << \SHAR_EOF
       cb3ba6949882a4d8546a2952155565c3  server/Dockerfile
       SHAR_EOF

       else
       test `LC_ALL=C wc -c < 'server/Dockerfile'` -ne 1593 && \
         ${echo} "restoration warning:  size of 'server/Dockerfile' is not 1593"
         fi
       fi
       # ============= ghmrva/Dockerfile ==============
       if test ! -d 'ghmrva'; then
         mkdir 'ghmrva'
       if test $? -eq 0
       then ${echo} "x - created directory ghmrva."
       else ${echo} "x - failed to create directory ghmrva."
            exit 1
       fi
       fi
       if test -n "${keep_file}" && test -f 'ghmrva/Dockerfile'
       then
       ${echo} "x - SKIPPING ghmrva/Dockerfile (file already exists)"

       else
       ${echo} "x - extracting ghmrva/Dockerfile (text)"
         sed 's/^X//' << 'SHAR_EOF' > 'ghmrva/Dockerfile' &&
       # ######################
       # Provide codeql and java
       # Binary built on host
       FROM ubuntu:24.04
       ENV DEBIAN_FRONTEND=noninteractive
       X
       # Build argument for CodeQL version, defaulting to the latest release
       ARG CODEQL_VERSION=latest
       X
       # Install packages
       RUN apt-get update && apt-get install --no-install-recommends --assume-yes \
       X    unzip \
       X    curl \
       X    ca-certificates \
       X    default-jdk
       X
       # If the version is 'latest', get the latest release version from GitHub, unzip
       # the bundle into /opt, and delete the archive
       RUN if [ "$CODEQL_VERSION" = "latest" ]; then \
       X    CODEQL_VERSION=$(curl -s https://api.github.com/repos/github/codeql-cli-binaries/releases/latest | grep '"tag_name"' | sed -E 's/.*"([^"]+)".*/\1/'); \
       X    fi && \
       X    echo "Using CodeQL version $CODEQL_VERSION" && \
       X    curl -L "https://github.com/github/codeql-cli-binaries/releases/download/$CODEQL_VERSION/codeql-linux64.zip" -o /tmp/codeql.zip && \
       X    unzip /tmp/codeql.zip -d /opt && \
       X    rm /tmp/codeql.zip && \
       X    chmod -R +x /opt/codeql
       X
       # Set environment variables for CodeQL
       ENV CODEQL_CLI_PATH=/opt/codeql/codeql
       X
       # Set environment variable for CodeQL for `codeql database analyze` support on ARM
       # This env var has no functional effect on CodeQL when running on x86_64 linux
       ENV CODEQL_JAVA_HOME=/usr
       X
       # ######################
       X
       # Set the working directory inside the final image
       WORKDIR /app
       X
       # Copy the host-built binary
       COPY gh-mrva /usr/local/bin/gh-mrva
       X
       # Put CodeQL on the PATH
       ENV PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/opt/codeql
       X
       # Run forever
       CMD ["tail", "-f", "/dev/null"]
       SHAR_EOF
         (set 20 26 01 12 14 04 09 'ghmrva/Dockerfile'
          eval "${shar_touch}") && \
         chmod 0664 'ghmrva/Dockerfile'
       if test $? -ne 0
       then ${echo} "restore of ghmrva/Dockerfile failed"
       fi
         if ${md5check}
         then (
              ${MD5SUM} -c >/dev/null 2>&1 || ${echo} 'ghmrva/Dockerfile': 'MD5 check failed'
              ) << \SHAR_EOF
       91fd8516408a065335fb24432e50f9b4  ghmrva/Dockerfile
       SHAR_EOF

       else
       test `LC_ALL=C wc -c < 'ghmrva/Dockerfile'` -ne 1591 && \
         ${echo} "restoration warning:  size of 'ghmrva/Dockerfile' is not 1591"
         fi
       fi
       # ============= hepc/Dockerfile ==============
       if test ! -d 'hepc'; then
         mkdir 'hepc'
       if test $? -eq 0
       then ${echo} "x - created directory hepc."
       else ${echo} "x - failed to create directory hepc."
            exit 1
       fi
       fi
       if test -n "${keep_file}" && test -f 'hepc/Dockerfile'
       then
       ${echo} "x - SKIPPING hepc/Dockerfile (file already exists)"

       else
       ${echo} "x - extracting hepc/Dockerfile (text)"
         sed 's/^X//' << 'SHAR_EOF' > 'hepc/Dockerfile' &&
       # Use a Python 3.11 image as the base
       FROM python:3.11-slim
       X
       # Create application root
       RUN mkdir -p /work-gh/mrva
       X
       # Copy source tree
       WORKDIR /work-gh/mrva
       COPY ./mrvahepc mrvahepc
       X
       # Switch to hepc source directory
       WORKDIR /work-gh/mrva/mrvahepc
       X
       # We're in a container: install globally
       RUN pip install --upgrade pip
       RUN pip install --no-cache-dir -r requirements.txt
       RUN pip install .
       X
       # Canonical values mount (authoritative, read-only at runtime)
       VOLUME /mrva/values
       X
       # Expose service port
       EXPOSE 8070
       X
       # Run global service (no path arguments)
       CMD ["hepc-serve-global", \
       X     "--host", "0.0.0.0", \
       X     "--port", "8070" \
       ]
       SHAR_EOF
         (set 20 26 01 12 14 04 09 'hepc/Dockerfile'
          eval "${shar_touch}") && \
         chmod 0664 'hepc/Dockerfile'
       if test $? -ne 0
       then ${echo} "restore of hepc/Dockerfile failed"
       fi
         if ${md5check}
         then (
              ${MD5SUM} -c >/dev/null 2>&1 || ${echo} 'hepc/Dockerfile': 'MD5 check failed'
              ) << \SHAR_EOF
       3eac48346165a2ea00f0687a6c348379  hepc/Dockerfile
       SHAR_EOF

       else
       test `LC_ALL=C wc -c < 'hepc/Dockerfile'` -ne 631 && \
         ${echo} "restoration warning:  size of 'hepc/Dockerfile' is not 631"
         fi
       fi
       if rm -fr ${lock_dir}
       then ${echo} "x - removed lock directory ${lock_dir}."
       else ${echo} "x - failed to remove lock directory ${lock_dir}."
            exit 1
       fi
       exit 0
     #+END_SRC

   - [X] add mrva-platform image with all tools
     use union of existing Dockerfiles
     
* TODO MRVA end-to-end test
  Test the new assembly 
  - scripts: [hohn@thelio24 ~/work-gh/mrva/mrva-system/scripts]$
  - Dockefiles:[hohn@thelio24 /mnt/common/home/hohn/work-gh/mrva/mrva-system/submodules/mrva-docker/containers]$

* TODO Pushing MRVA containers
  
